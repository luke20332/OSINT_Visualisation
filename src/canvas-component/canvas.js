const Canvas = ({
    width,
    height
}) => {
    return (
        <canvas
            width={width}
            height={height}/>
    );
}

export default Canvas;

const canvasStyle = {
    border: '1px solid black'
}